package ma.toto.search.utils;

/** The interface Date validator. */
public interface DateValidator {

  /**
   * Is valid boolean.
   *
   * @param dateString the date string
   * @return the boolean
   */
  boolean isValid(String dateString);
}
